User-agent: *
Allow: /
# The app shell: /app itself and everything under it. Anchored, so /apply/*
# stays crawlable (a bare "Disallow: /app" would match it as a prefix).
Disallow: /app$
Disallow: /app/
# /login, /signup and /reset-password are crawlable on purpose: each answers
# with `noindex`, which a crawler can only read if it may fetch the page.

Sitemap: https://www.founders.click/sitemap.xml
Sitemap: https://www.founders.click/help/sitemap.xml
